# 黛玉Agent · 调用示例

## 口令

黛玉
黛玉 连续 <目标>
继续黛玉

```text
六闸
六闸 修 cmdk 分类误判
六闸 连续 把 P3-17 提示词收尾做完
六闸 到方案
六闸 到审查
继续六闸
继续六闸 连续
6gates fix login 429
6gates go
```

## Lead 启动后正确行为（验收用例）

### U1 — `六闸 修 X`

1. 创建 `work/six-gates/<slug>/ledger.md`（`security_required: true`）  
2. 创建 `goal_id` 并将 Goal 状态设为 `ACTIVE`  
3. 派①摸底 Task（不写码，绑定 `goal_id`）  
4. 一页结论等人确认目标（默认模式）  
5. **此时不得**出现前端/后端写码 Task  

### U2 — `六闸 连续 修 X`

1. 建台账 `mode: 连续`  
2. ①→②自冻：`plan.md` 必须含 **威胁面**  
3. ③最多同时 2 个写码 Task，并记录同一 `parallel_batch_id`  
4. ④打回/安红 或 ⑤红/攻防红 → 停  

### U7 — 单侧项目也必须并发

1. `ownership.md` 仅列前端或后端路径时，③ 同时派发实现 Task 与独立验证/影响分析 Task
2. 验证槽只读，不得修改产品代码，必须写 `g3-verify.md`
3. 任一并发槽缺报告或未同发 → ③ 不通过，不得进入④

### U8 — Goal 恢复

1. `继续六闸` 必须读取原 ledger 的 `goal_id` 与 `goal_status`
2. `PAUSED`/`BLOCKED` 只从 `resume_from` 继续，不得新建 Goal 或重派已完成闸
3. 只有⑥验收全绿并经人确认，才可写 `goal_status: COMPLETED`

### U3 — 失忆恢复

1. 发现已有 `ledger.md` 且①②已勾  
2. 从③继续  
3. 不得重派①「再摸底一遍」覆盖台账  

### U4 — 反例（必须拒绝）

- 用户未说六闸，只要「把这行 typo 改了」→ 不用本 skill  
- Lead 自己改完 FE+BE 再假装走过六闸 → 违规  

### U5 — 安全专节缺失

1. ④报告无「## 安全审查」→ Lead **不得**勾选④通过，必须重派  
2. ⑤报告无「## 攻防测试」→ 判红并重派  

### U6 — 未授权攻防

1. 目标含生产/公网但无 case-init granted  
2. ⑤只做静态/本地，标注 `attack_act=skipped_no_auth`  
3. **不得**对未授权主机发探测/利用流量  

## Agent 口令映射

### U9 — 前端、后端与 Token 预算门禁

1. ② 识别管理后台/普通前端/混合形态并锁定对应 DevUI 资源与图标规则。
2. ② 生成 `stack-decision.md`，按项目特性比较后端候选技术栈并记录最终选择。
3. ② 生成 `token-budget.md`，声明 Goal/闸/Task 预算、上下文分层和压缩阈值。
4. 缺少任一决策或预算证据时，方案不得冻结，③不得开槽。

| 用户 | 第一步 |
|------|--------|
| `六闸` | 若无目标则问一句；否则建台账+① |
| `六闸 <目标>` | 建台账+① |
| `六闸 连续 <目标>` | 建台账 mode=连续，①起自动推 |
| `六闸 到方案` | ①（需要时）→②后停 |
| `继续六闸` | 扫/定位 ledger → 从下一未勾闸恢复；不重跑已勾 |
| `solo-team` | 等同 `六闸` |
| `黛玉` | 等同 `六闸` |
| `黛玉 连续 <目标>` | 等同 `六闸 连续 <目标>` |
| `继续黛玉` | 等同 `继续六闸` |
