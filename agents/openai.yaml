interface:
  display_name: "黛玉Agent"
  short_description: "Goal驱动的多Agent并发开发工作流（独立审查与Token装箱）"
  brand_color: "#8B3A62"
  default_prompt: "Use $daiyu-agent to coordinate this software task with Goal tracking, parallel agents, independent quality review, and verified delivery."
policy:
  allow_implicit_invocation: true
