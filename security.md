# 黛玉Agent · 安全审查与攻防测试（强制）

④⑤ 的安全内容**不可跳过**。本文件是清单真相源。

## 授权边界（MUST）

| 动作 | 条件 |
|------|------|
| 静态安全审查（读 diff/配置/密钥形态） | 始终允许（④） |
| 本地/仓库内单元与冒烟 | 始终允许（⑤功能） |
| 对本机授权服务、已 grant 的 lab、offline-sample | ⑤攻防允许 |
| 对公网、生产、第三方、未授权主机做探测/利用 | **禁止**，除非人明确授权且 reverse-skill `case-init` `auth.status=granted` + 合法 `network_profile` |
| 写恶意后门、无范围扩大攻击面 | **禁止** |

仓库若存在 `reverse-skill`：攻防 ACT 前跑 case-init；未 granted → ⑤攻防只做「静态滥用用例推导 + 本地可复现项」，并在报告标注 `attack_act=skipped_no_auth`。

## ② 方案必须写的威胁面

`plan.md` 增加一节（可短）：

```markdown
## 威胁面
- 资产: …（API/token/代理/上传/管理端…）
- 信任边界: …
- 滥用用例: …（越权、注入、密钥泄露、SSRF、未授权转发…）
- 攻防范围: 静态-only | 本地 | 已授权目标 <url/host>
- 非目标: …
```

无「威胁面」→ ②不得自冻（连续模式也不得进③）。

## ④ 安全审查专节（阻塞）

报告 `g4-review.md` **必须**含：

```markdown
## 安全审查
### 结论
过 | 打回安全 | 打回前端 | 打回后端 | 打回方案
### 检查项
- [ ] 密钥/token/凭证是否入库或日志泄露
- [ ] 鉴权/授权是否可绕过或默认放开
- [ ] 输入校验 / 注入面（命令、SQL、路径、模板）
- [ ] SSRF / 开放代理 / 未鉴权转发
- [ ] 危险配置默认值（debug、CORS *、明文）
- [ ] 依赖与供应链明显风险（若本批引入）
- [ ] 与 plan 威胁面是否对齐；新增攻击面是否未声明
### 发现
| ID | 严重度 | 描述 | 证据 | 建议归属 |
|----|--------|------|------|----------|
### 残留风险
…
```

任一 **Critical/High** 未给出可接受残留说明 → 不得「过」。  
仅风格问题不阻塞；安全问题用标签 `打回安全`（或打回前后端）。

## ⑤ 攻防测试专节（阻塞）

报告 `g5-test.md` **必须**含功能结果 **与**：

```markdown
## 功能测试
…（命令、退出码）

## 攻防测试
### 范围
静态-only | 本地 | 已授权 <target>（附 auth 证据路径）
### 用例
| ID | 用例 | 步骤 | 期望 | 实际 | 结果 |
|----|------|------|------|------|------|
| A1 | … | … | 拒绝/无泄露 | … | 绿/红 |
### 结论
攻防绿 | 攻防红
### 归属（若红）
打回安全 | 打回前端 | 打回后端 | 打回方案 | 仅修测障
```

最低用例（按本批裁剪，但至少 1 条与威胁面相关；无网络面则做静态滥用核对）：

1. 未授权访问敏感接口 / 管理路径  
2. 越权读写（若有用户/租户）  
3. 注入或路径穿越（若有用户输入）  
4. 密钥与错误信息泄露  
5. 代理/转发类：目标限制与鉴权（若本批改 proxy/relay）

未跑攻防却写绿 → Lead 必须判⑤红并重派。

### 未授权时何时可判「攻防绿」

当 `attack_act=skipped_no_auth` 时，**允许**判攻防绿，当且仅当同时满足：

1. 已按威胁面完成 **静态滥用核对表**（每条有「风险是否在本 diff 引入/暴露」结论）  
2. 凡本机可复现项（配置默认、单测能表达的鉴权拒绝等）已真跑并记录退出码/结果  
3. 报告写明：**残留风险 = 未做远程 ACT**；⑥收口必须把该残留抄进 `g6-close.md`  
4. 人未要求「必须打授权目标」——若人明确要求远程攻防而仍无授权 → **停，BLOCKED**，不得绿

不得把「跳过」当成「无风险」。

## 与功能审查/测试的关系

```text
④ = 质量对照验收 + 安全审查（同一闸，可一 Task 两专节）
⑤ = 功能回归真跑 + 攻防用例（同一闸，可一 Task 两专节）
```

不要拆成第七闸；不要用「以后再做安全」关单。

## 修复单

安全/攻防红 → `batch: fix-<n>`，标签优先 `打回安全`，复验必须重跑④安全专节与⑤攻防专节相关项。
